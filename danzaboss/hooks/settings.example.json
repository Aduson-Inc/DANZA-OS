{
  "_comment": "EXAMPLE Claude Code hooks wiring (mirrors the live .claude/settings.json). VERIFY event names + return contract against the current Claude Code hooks docs before use; this is the integration surface, the guard/gate logic is in the tested Python modules.",
  "hooks": {
    "PreToolUse": [
      {"matcher": "Write|Edit|MultiEdit|Bash",
       "hooks": [{"type": "command", "command": "PYTHONPATH=\"${CLAUDE_PROJECT_DIR}\" python3 -m danzaboss.cli hook pretooluse"}]}
    ],
    "SessionStart": [
      {"hooks": [{"type": "command", "command": "PYTHONPATH=\"${CLAUDE_PROJECT_DIR}\" python3 -m danzaboss.cli cortex hook session-start"}]}
    ],
    "PostToolUse": [
      {"matcher": "Write|Edit|Bash",
       "hooks": [{"type": "command", "command": "PYTHONPATH=\"${CLAUDE_PROJECT_DIR}\" python3 -m danzaboss.cli cortex hook post-tool-use"}]}
    ],
    "Stop": [
      {"hooks": [{"type": "command", "command": "PYTHONPATH=\"${CLAUDE_PROJECT_DIR}\" python3 -m danzaboss.cli cortex hook stop"}]}
    ]
  }
}
